import {
  Meta,
  Story,
  Preview,
  Props,
  Source,
  SourceState,
} from '@storybook/addon-docs/blocks';
import { withKnobs, text, boolean } from '@storybook/addon-knobs';
import { action } from '@storybook/addon-actions';
import { withA11y } from '@storybook/addon-a11y';
import Button from '../src/components/Button';

<Meta
  title="Atoms/Button"
  decorators={[withKnobs({ escapeHTML: false }), withA11y]}
/>

# Button components

기본 버튼 컴포넌트 입니다.

```js
<Button selected={[Boolean | false]} disabled={[Boolean | false]}>
  ChildNode
</Button>
```

# Preview

<Preview>
  <Story name="Default">
    <Button onClick={action('default clicked')}>
      Default
    </Button>
  </Story>
</Preview>

<Preview>
  <Story name="Selected">
    <Button onClick={action('selected clicked')}>
      Selected
    </Button>
  </Story>
</Preview>

<Preview>
  <Story name="Disabled">
    <Button onClick={action('disabled clicked')}>
      Disabled
    </Button>
  </Story>
</Preview>

# Props

<Props of={Button} />